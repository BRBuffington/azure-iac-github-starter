# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.json
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Backend config with real values (use backend.hcl.example as the template)
infra/backend.hcl

# Filled-in MCP config (use .vscode/mcp.json.example as the template)
.vscode/mcp.json

# Local env
*.tfvars.local
.env

# Python (the .github/scripts helpers)
__pycache__/
*.pyc
